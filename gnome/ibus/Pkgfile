# Description: IBus is an Intelligent Input Bus. It is a new input framework for Linux OS. It provides full featured and user friendly input method user interface.
# URL: http://ibus.googlecode.com
# Maintainer: http://linuxfromscratch.org/
# Packager: thierryn1 at hispeed dot ch
# Depends on: dconf iso-codes gobject-introspection gtk2 libnotify vala python3 python-gobject  libxkbcommon dbus-python python-xdg

description="Intelligent Input Bus, a new input framework for Linux OS."
name=ibus
version=1.5.12
release=1

source=(https://github.com/ibus/ibus/releases/download/$version/$name-$version.tar.gz)
build() {
cd $name-$version
./configure --prefix=/usr \
            --sysconfdir=/etc \
            --enable-python-library \
            --with-python=python3 \
            --enable-gtk-doc
            
make
make DESTDIR=$PKG install

}
