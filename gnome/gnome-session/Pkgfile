# Description: The GNOME Session Handler
# URL: http://www.gnome.org/
# Maintainer: git.gnome.org/brownse/gnome-session/
# Packager: tnut at nutyx dot org
# Depends on: hicolor-icon-theme gnome-desktop json-glib consolekit

run=(hicolor-icon-theme consolekit)

name=gnome-session
version=3.16.0
release=1
source=(http://ftp.gnome.org/pub/gnome/sources/$name/${version%.*}/$name-$version.tar.xz timeout.patch)

build() {
cd $name-$version
patch -Np1 -i ../timeout.patch
./configure --prefix=/usr \
--sysconfdir=/etc \
--localstatedir=/var \
--libexecdir=/usr/lib/$name \
--disable-schemas-compile \
--disable-gconf
make
make DESTDIR=$PKG install
} 
