# Description: GDM is a system service that is responsible for providing graphical logins and managing local and remote displays.
# URL: http://www.gnome.org/
# Packager: tnut at nutyx dot org, fanch at nutyx dot org
# Depends on: accountsservice nss libcanberra gnome-shell gnome-session upower

description="Responsible for providing graphical logins and managing local and remote displays."
name=gdm
version=3.16.4
release=1
source=(http://ftp.gnome.org/pub/gnome/sources/$name/${version%.*}/$name-$version.tar.xz)

build() {
cd $name-$version
./configure --prefix=/usr        \
            --sysconfdir=/etc    \
            --localstatedir=/var \
            --disable-static  \
            --without-plymouth \
			--without-systemd \
			--disable-schemas-compile \
			--without-tcp-wrappers \
			--without-console-kit
  make
make DESTDIR=$PKG install
}

