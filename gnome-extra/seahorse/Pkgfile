# Description: GNOME application for managing PGP keys.
# URL: http://www.gnome.org/
# Maintainer: http://linuxfromscratch.org/
# Packager: thierryn1 at hispeed dot ch
# Depends on: avahi gcr gnome-keyring gpgme gnupg itstool libsecret libsoup desktop-file-utils openssh openldap gnome-doc-utils gobject-introspection vala

name=seahorse
version=3.16.0
release=1
source=(http://download.gnome.org/sources/$name/${version%.*}/$name-$version.tar.xz)

build() {
	cd $name-$version
	./configure --prefix=/usr     \
            --bindir=/usr/bin \
            --disable-static
	make
	make DESTDIR=$PKG install
}
