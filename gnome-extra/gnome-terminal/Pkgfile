# Description: The GNOME Terminal Emulator.
# URL: http://www.gnome.org/
# Maintainer: git.gnome.org/brownse/gnome-terminal/
# Packager: thierryn1 at hispeed dot ch
# Depends on: desktop-file-utils, dconf, gconf, gtk3, gnome-doc-utils, gsettings-desktop-schemas, vte3, yelp-xsl, rarian, nautilus

name=gnome-terminal
version=3.16.2
release=1
source=(http://ftp.gnome.org/pub/gnome/sources/$name/${version%.*}/$name-$version.tar.xz)

build() {
cd $name-$version
./configure --prefix=/usr \
    --disable-static --disable-migration --disable-search-provider
make
make DESTDIR=$PKG install
sed -e 's/System/Utility/' \
    -i $PKG/usr/share/applications/gnome-terminal.desktop
}
